# Local agent/editor config — never published
**/.claude/settings.local.json

# Astro Platform build artifacts and toolchain
dist/
node_modules/
.astro/

# Service account keys and local env — credentials never get committed
secrets/
*.sa.json
.env
.env.*
