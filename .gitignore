# Local agent/editor config — never published
**/.claude/settings.local.json

# Ephemeral build output (the index is a pure function of the source tree)
_site/

# Astro Platform build artifacts and toolchain
dist/
node_modules/
.astro/
